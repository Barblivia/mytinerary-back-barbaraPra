const controller = {
    getUsers: (req, res) => {
        res.json({
            user: 'Barbara Livia Pra'
        });
    },
    postUser: () => {},
    deleteUser: () => {},
}

export default controller;